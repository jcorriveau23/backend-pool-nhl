[package]
name = "poolnhl_app"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
poolnhl_infrastructure = { path = "../poolnhl_infrastructure" }
poolnhl_routing = { path = "../poolnhl_routing" }
tokio = "1.38"
